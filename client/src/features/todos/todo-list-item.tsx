import * as React from "react";
import * as Redux from "react-redux";
/*
import { availableColors, capitalize } from "../filters/colors";

function selectTodoById(state: any, todoId?: number) {
  return state.todos.find((todo: any) => todo.id === todoId);
}

export function TodoListItem({ id }: { id?: number }) {
  const todo = Redux.useSelector((state) => selectTodoById(state, id));
  const { text, completed, color } = todo;

  const dispatch = Redux.useDispatch();

  function handleCompletedChanged(e: any) {
    dispatch({ type: "todos/todosToggled", payload: todo.id });
  }

  return (
    <li>
      <div className="view"></div>
    </li>
  );
}
*/
